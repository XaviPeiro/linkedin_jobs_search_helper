user:
password:

openai_api:
  secret:

discard_job:
  # !IMPORTANT
  # Set on the second line what it your criteria in natural language.
  # If the response to your question is YES it will be discarded.
  criteria: > 
    Assuming this job description: \"{}\".
    Will I be discarded from this selection process if I intend to work remotely from Poland? 
    This is a yes-no question, I do not want you to respond anything else than yes or no.